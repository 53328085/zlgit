import {createContext} from 'react'

export const ageContext = createContext(30);

export const jobContext = createContext('web工程师')
export const levelContext = createContext(0)

export const objCOntext = createContext({})